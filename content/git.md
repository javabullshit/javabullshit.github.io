Title: Git usege
Date: 2020-09-28
Category: Web

```
git init
git add --all
#git add output
git commit -m "first commit"
git remote add origin url
git push -u origin master
```
拉取指定分支：
```bash
git clone -b 分支名称 URL
```
